"""Adapters that let external harnesses drive the council.

Importing a submodule here pulls in that harness's package (e.g. ``slm_bench`` imports
``slm_coding_bench``); the council core itself stays free of those dependencies.
"""
